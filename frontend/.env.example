# In development, leave VITE_API_URL empty - the Vite dev server proxies
# /api to http://localhost:4000 (see vite.config.js).
#
# In production, set this to your deployed backend origin, e.g.
# VITE_API_URL=https://acowale-api.onrender.com
VITE_API_URL=
